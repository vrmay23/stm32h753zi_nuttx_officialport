include $(APPDIR)/Make.defs

PROGNAME  = $(CONFIG_EXAMPLES_CANIO_PROGNAME)
PRIORITY  = $(CONFIG_EXAMPLES_CANIO_PRIORITY)
STACKSIZE = $(CONFIG_EXAMPLES_CANIO_STACKSIZE)
MODULE    = $(CONFIG_EXAMPLES_CANIO)

MAINSRC = canio_main.c

include $(APPDIR)/Application.mk
