if(CONFIG_EXAMPLES_CANIO)
  nuttx_add_application(
    NAME
    ${CONFIG_EXAMPLES_CANIO_PROGNAME}
    PRIORITY
    ${CONFIG_EXAMPLES_CANIO_PRIORITY}
    STACKSIZE
    ${CONFIG_EXAMPLES_CANIO_STACKSIZE}
    MODULE
    ${CONFIG_EXAMPLES_CANIO}
    SRCS
    canio_main.c)
endif()
